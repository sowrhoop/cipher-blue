# Space-separated list of allowed VPN interfaces
ALLOWED_IFACES="wg0 tun0 tap0"

# Optional: set to 1 to disable the killswitch without removing the unit
# Note: changes take effect next time the service starts; to apply immediately:
#   sudo systemctl restart cipher-killswitch
DISABLED=0
